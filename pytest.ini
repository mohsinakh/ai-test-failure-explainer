[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    login: Login module tests
    dashboard: Dashboard module tests
    api: REST API module tests
    demo_failure: Intentionally-failing tests used to exercise the AI failure explainer. Not real regressions - see tests/demo_failures/test_demo_failures.py.